#include<iostream>

//找两个数组的中位数 

using namespace std;

void prePart(int &s,int &t)
{
	int m=(s+t)/2;
	t=m;
 }
void postPart(int &s,int &t)
{
	int mid=(s+t)/2;
	if((s+t)%2==0) t=mid;
	else t=mid+1;
 } 
 
int midNum(int a[],int sa,int ta,int b[],int sb,int tb)
{
	int midA=(sa+)
	if(a[])
	if(a[sa]==a[ta]&&b[sb]==b[tb])
	{
		return a[sa]<b[sb]?a[sa]:b[sb];
	}
	else if(sa>ta&&sb>tb)
		return -1;
	else
	{
		mid=(s1+t1)/2;	
	}
}

int main()
{
	int test[6]={2,5,1,4,6,3};
	int n=sizeof(test)/sizeof(test[0]);
	
 } 
